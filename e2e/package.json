{
  "name": "e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install:browsers": "playwright install --with-deps chromium",
    "scrape": "ts-node --project tsconfig.json scrape-chogan.ts",
    "audit:ui": "node visual/audit.mjs",
    "audit:home": "node visual/home-sections.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^22.20.1",
    "axe-core": "^4.13.0",
    "ts-node": "^10.9.2"
  }
}
